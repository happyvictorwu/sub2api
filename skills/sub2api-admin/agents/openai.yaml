interface:
  display_name: "Slothwatching Admin"
  short_description: "Manage Slothwatching admin accounts and settings"
  default_prompt: "Use $slothwatching-admin to inspect and manage Slothwatching backend accounts safely."

policy:
  allow_implicit_invocation: true
